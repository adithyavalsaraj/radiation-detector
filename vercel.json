{
  "installCommand": "npm install --no-package-lock",
  "buildCommand": "npm run build --workspace=frontend && rm -rf dist && mv frontend/dist dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
